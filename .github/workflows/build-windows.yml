name: Build Windows EXE

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build EXE
        run: pyinstaller --onefile --windowed --name "EtiquetaTron" --icon "icon.ico" --add-data "logo.png;." --add-data "icon.ico;." main.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: EtiquetaTron-Windows
          path: dist/EtiquetaTron.exe
